{
  "name": "@unique-ag/logger",
  "version": "0.3.0",
  "private": true,
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "format": "biome format .",
    "format:fix": "biome format . --write",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "pino-pretty": "^13.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "typescript": "^5.9.2"
  },
  "peerDependencies": {
    "@nestjs/common": "^10 || ^11",
    "nestjs-pino": "^4.4.0",
    "pino-http": "^10.5.0"
  }
}
