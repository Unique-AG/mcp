DATABASE_URL=postgresql://postgres:postgres@localhost:5432/outlook-mcp
MICROSOFT_CLIENT_ID=<azure-ad-app-client-id>
MICROSOFT_CLIENT_SECRET=<azure-ad-app-client-secret>
JWT_SECRET=<jwt-secret> # Random 32-char secret. Create with e.g. openssl rand -hex 32
ENCRYPTION_KEY=<encryption-key> # Random 32-char secret. Create with e.g. openssl rand -hex 32