name: "[mcp/server/factset] Warm Docker Build Cache (nightly)"

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch: {}

jobs:
  warm:
    uses: ./.github/workflows/_template-mcp-cache.yaml
    with:
      service-name: factset
      service-path: services/factset-mcp